package mentorgroupimportantquestions;

public class Questions {
    public static void main(String[] args) {

         /*
          Question 1)
          Ebay Interview Question!!!!
          Put all zeros to end in an integer array
          {3, 0, 4, 2, 0} ==> {3, 4, 2, 0, 0}
          {0, 0, 1, 2, 0, 3} ==> {1, 2, 3, 0, 0, 0}
         */


        //Q02: If the number of characters is more than 4, remove them from the LinkedList
        //Interview Question

//        LinkedList<String> name = new LinkedList<>();
//        name.add("Tom");
//        name.add("Jane");
//        name.add("Mary");
//        name.add("Mahmut");
//        name.add("Ali");
//        name.add("Emily");
//
//        for(int i =0; i<name.size(); i++){
//
//            if(name.get(i).length()>4){
//                name.remove(name.get(i));
//                i--;             //since the indexes will change after every iteration
//            }
//        }
//        System.out.println(name);// [Tom, Jane, Mary, Ali]




        // 3)Create a method called displayHighScorePosition
        // it should have the player name as a parameter and the 2nd parameter as a position in the high-score table
        // You should display the player's name along with a message like " managed to get into position " and the
        // position they got and a further message " on the high score table".
        //
        // Create a 2nd method called calculateHighScorePosition
        // it should be sent one argument only, the player's score
        // it should return an in
        // the return data should be
        // 1 if the score is >=1000
        // 2 if the score is >=500 and < 1000
        // 3 if the score is >=100 and < 500
        // 4 in all other cases
        // call both methods and display the results of the following
        // a score of 1500, 900, 400 and 50
        //














    }
}
